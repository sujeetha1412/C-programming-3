#include<stdio.h>
int main()
{
   int n,original,c=0,r;
   printf("Enter a number:");
   scanf("%d",&n);
   original=n;
   while(n>0)
   {
      r=n%10;
      c=c+1;
      n=n/10;
   }
   printf("Number of digits present in %d is %d",original,c);
   printf("\nfinished");
   return 0;
}
